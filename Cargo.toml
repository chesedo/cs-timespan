[package]
name = "cs-timespan"
version = "0.1.0"
edition = "2024"

[lib]
bench = false

[features]
default = []
chrono = ["dep:chrono"]

[dependencies]
num-format = "0.4"
chrono = { version = "0.4", optional = true }

[dev-dependencies]
criterion = "0.8"

[[bench]]
name = "format"
harness = false
